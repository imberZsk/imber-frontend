---
description: 页面级私有组件规则（适用于 app/**/_components/**）
globs:
  - 'app/**/_components/**'
---

对于 `app/**/_components/**` 下的文件：

- 将组件视为路由作用域内的，而非全局的；优先使用本地组合而非导出到全局 `components/`。
- 保持 props 最小且稳定；当逻辑被复用时，将共享逻辑移至 `utils/` 或全局组件。
- 除非性能分析证明有必要，否则避免使用 `useMemo`/`useCallback`；优先拆分和简化。
- 确保无障碍性（角色、标签、键盘导航）。
- 默认使用默认导出；仅在需要多个相关导出时使用命名导出。
